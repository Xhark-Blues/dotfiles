{
  "name": "ts-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "setup": "npm i && tsc --outDir dist",
    "start": "nodemon --watch src --watch package.json -e ts,json -q -x 'rm -fr dist && echo \"\\n============\\nCOMPILING...\\n============\\n\\n\" && tsc --outDir dist || return 0 && node dist/cli.js'",
    "test": "echo 'no test suite defined' && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "@types/bluebird": "^3.0.36",
    "@types/moment": "^2.13.0",
    "@types/node": "^6.0.51",
    "bluebird": "^3.4.6",
    "dockerode": "^2.3.1",
    "linux-battery": "^3.0.1",
    "moment": "^2.17.0",
    "network": "^0.3.0"
  }
}
